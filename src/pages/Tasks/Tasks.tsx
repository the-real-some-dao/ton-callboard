import { Page } from '../../components/Page'
import { PageType } from '../constants'

export const Tasks = () => {
  return (
    <Page type={PageType.Tasks} headerProps={{}} >
      Tasks
    </Page>
  )
}